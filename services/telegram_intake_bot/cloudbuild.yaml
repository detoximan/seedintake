steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - services/telegram_intake_bot/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: gcr.io/$PROJECT_ID/seedintake-telegram-bot
