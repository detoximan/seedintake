# Журнал работ (Worklog) — SeedIntake Link Worker

## Инструкция для LLM-агентов (Прочитать при входе!)
Этот файл содержит хронологический лог выполнения задач. При старте нового чата/сессии **читайте только последнюю секцию лога**, чтобы мгновенно понять:
1. Что уже сделано.
2. Что сейчас находится в процессе.
3. Какой следующий конкретный шаг нужно выполнить.

---

### [2026-07-25 15:26 UTC] Инициализация рефакторинга Link Worker

- **Контекст:** Принято решение перестроить `link_worker` с монолитного `UniversalMediaProcessor` на модульную архитектуру `URL Router + Specialized Workers`.
- **Что сделано:**
  1. Проанализирована текущая архитектура `SeedIntake` (`AGENTS.md`, `worker.py`, `processors.py`, `queue.py`, `cli.py`, `router.py`).
  2. Выявлены причины сбоев при обработке Instagram (блокировка `yt-dlp`, задержки в 15 секунд, ненадёжная попытка чтения куков Firefox через `browser_cookie3` при открытом браузере).
  3. Составлен и утвержден план работ в файле `2026-07-25-workplan.md`.
  4. Созданы файлы `2026-07-25-workplan.md` и `2026-07-25-worklog.md`.
- **Текущий статус:** Выполнены Шаги 1 и 2.

---

### [2026-07-25 15:27 UTC] Выполнен Шаг 2 (Создание URL Router)

- **Что сделано:**
  1. Написан класс `LinkRouter` в `router.py`.
  2. Добавлен enum `LinkPlatformType` для точной классификации всех популярных типов ссылок: `instagram_reels`, `instagram_carousel`, `tiktok_video`, `tiktok_photo`, `youtube_shorts`, `facebook_post`, `threads_post`, `text_post`.
  3. Классификация работает мгновенно без обращения в сеть по доменам и путям URL.
- **Текущий статус:** Выполнен Шаг 2.
- **Следующий шаг:** Выполнить Шаг 3 — Создать отдельный специализированный воркер для Instagram (`processors/instagram.py`) с корректной поддержкой загрузки куков из файла и разделением на Reels (видео) и Carousel/Posts (картинки + OCR).


---

### [2026-07-25 15:32 UTC] Выполнены Шаги 3 и 4 (Специализированные воркеры и Диспетчер)

- **Что сделано:**
  1. Созданы модули в `link_worker/processors/`:
     - `instagram.py`: раздельная обработка Reels (Whisper) и Каруселей/Фото (Instaloader/OCR).
     - `tiktok.py`: поддержка видео и фото-слайдшоу через `gallery-dl`.
     - `youtube.py`: обработка Shorts.
     - `web.py`: статьи и посты.
     - `__init__.py`: экспорт воркеров.
  2. Обновлен `PlatformLinkProcessor` в `processors.py`: теперь выбор исполнителя происходит через `LinkRouter.resolve_platform(item.url)`.
- **Текущий статус:** Выполнены Шаги 1, 2, 3, 4.

---

### [2026-07-25 15:41 UTC] Выполнен Шаг 5 (Тестирование и завершение рефакторинга)

- **Что сделано:**
  1. Добавлен набор unit-тестов для `LinkRouter` в `services/seed_pipeline/tests/link_worker/test_router.py`.
  2. Проведены тесты:
     - `seed_pipeline`: 34/34 тестов пройдены успешно (`OK`).
     - `telegram_intake_bot`: 52/52 тестов пройдены успешно (`OK`).
     - `test_router`: 4/4 тестов пройдены успешно (`OK`).
  3. Проверено состояние очереди ссылок (`link-worker list`).
- **Текущий статус:** Все шаги плана `2026-07-25-workplan.md` и исправления внедрены.

---

### [2026-07-25 15:45 UTC] Исправление процессоров платформ и статичные куки

- **Что сделано:**
  1. Устранены ошибки синтаксиса (`IndentationError`) в `tiktok.py` и `youtube.py`.
  2. Исправлено форматирование переносов строк (`\n`) в выводах процессоров Instagram и TikTok.
  3. Реализован загрузчик куки из файлов `.cookies/*.txt` (`get_cookies_file`) без зависимости от чтения SQLite браузера Chrome/Firefox через `browser_cookie3`.
  4. Настроена прозрачная передача `use_cookies` и `rate_limiter` через `PlatformLinkProcessor` ко всем процессорам платформ.
  5. Все автотесты пройдены (100% PASS).
- **Текущий статус:** Проект полностью готов к обработке ссылок.


